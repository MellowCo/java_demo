<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.li.mp.dao.MpUserMapper">
    <select id="selectAll" resultType="com.li.mp.domain.MpUser">
        select id,name from mp_user ${ew.customSqlSegment}
    </select>

    <select id="myPage" resultType="com.li.mp.domain.MpUser">
        select id,name from mp_user ${ew.customSqlSegment}
    </select>
</mapper>